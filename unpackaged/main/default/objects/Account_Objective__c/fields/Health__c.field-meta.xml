<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Health__c</fullName>
    <externalId>false</externalId>
    <formula>IF(AND(TEXT(Status__c) = &quot;Not Started&quot;, Due_Date__c &gt; TODAY()), 1, 
IF(AND(TEXT(Status__c) = &quot;Not Started&quot;, Due_Date__c &lt; TODAY()), -2, 
IF(AND(TEXT(Status__c) = &quot;In Progress&quot;, Due_Date__c &gt; TODAY()), 2, 
IF(AND(TEXT(Status__c) = &quot;In Progress&quot;, Due_Date__c &lt; TODAY()), 0, 
IF(AND(TEXT(Status__c) = &quot;Ongoing&quot;, Due_Date__c &gt; TODAY()), 2, 
IF(AND(TEXT(Status__c) = &quot;Ongoing&quot;, Due_Date__c &lt; TODAY()), 0, 
IF(TEXT(Status__c) = &quot;Completed&quot;, 5, 0)))))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Health</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
