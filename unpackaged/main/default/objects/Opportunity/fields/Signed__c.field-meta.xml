<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Signed__c</fullName>
    <externalId>false</externalId>
    <formula>IF(AND(Contract_Start_Date_Month__c = 1, IsWon = TRUE), TF_Annual_NC__c * 1,
IF(AND(Contract_Start_Date_Month__c = 2, IsWon = TRUE), TF_Annual_NC__c * 0.9331973,
IF(AND(Contract_Start_Date_Month__c = 3, IsWon = TRUE), TF_Annual_NC__c * 0.8783086,
IF(AND(Contract_Start_Date_Month__c = 4, IsWon = TRUE), TF_Annual_NC__c * 0.8201378,
IF(AND(Contract_Start_Date_Month__c = 5, IsWon = TRUE), TF_Annual_NC__c * 0.7555707,
IF(AND(Contract_Start_Date_Month__c = 6, IsWon = TRUE), TF_Annual_NC__c * 0.6816043,
IF(AND(Contract_Start_Date_Month__c = 7, IsWon = TRUE), TF_Annual_NC__c * 0.5999116,
IF(AND(Contract_Start_Date_Month__c = 8, IsWon = TRUE), TF_Annual_NC__c * 0.5001037,
IF(AND(Contract_Start_Date_Month__c = 9, IsWon = TRUE), TF_Annual_NC__c * 0.379803,
IF(AND(Contract_Start_Date_Month__c = 10, IsWon = TRUE), TF_Annual_NC__c * 0.2700752,
IF(AND(Contract_Start_Date_Month__c = 11, IsWon = TRUE), TF_Annual_NC__c * 0.1573122,
IF(AND(Contract_Start_Date_Month__c = 12, IsWon = TRUE), TF_Annual_NC__c * 0.0752389,0))))))))))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Signed</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
